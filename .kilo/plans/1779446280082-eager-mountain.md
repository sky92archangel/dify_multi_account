# Plan: 修改 skip_reg_code_plan.md 默认密码

## 分析

`plans/skip_reg_code_plan.md` 是一份技术方案文档，描述了 Dify 免验证码一键注册功能的后端+前端改造方案。文档中所有默认密码均为 `1234@1234`。

## 修改内容

将 `plans/skip_reg_code_plan.md` 中所有 `1234@1234` 替换为 `admin123`。共涉及以下位置：

| 行号 | 原文 | 替换为 |
|------|------|--------|
| 5 | `默认密码为 \`1234@1234\`` | `默认密码为 \`admin123\`` |
| 44 | `创建用户(密码=1234@1234)` | `创建用户(密码=admin123)` |
| 68 | `默认密码 1234@1234` | `默认密码 admin123` |
| 77 | `密码使用默认值 "1234@1234"` | `密码使用默认值 "admin123"` |
| 81 | `password=DEFAULT_PASSWORD,  # "1234@1234"` | `password=DEFAULT_PASSWORD,  # "admin123"` |
| 119 | `1234@1234` 不符合密码规则 | `admin123` 不符合密码规则 |
| 121 | 当密码为 `1234@1234` 时跳过 | 当密码为 `admin123` 时跳过 |
| 127, 134 | `1234@1234` 跳过复杂校验 | `admin123` 跳过复杂校验 |
| 141, 143, 144 | `DEFAULT_REGISTER_PASSWORD = "1234@1234"` | `DEFAULT_REGISTER_PASSWORD = "admin123"` |
| 221, 222 | 清单中的 `1234@1234` 引用 | `admin123` |
| 231, 234 | 密码 `1234@1234` 的安全性注解 | `admin123` |
| 245 | `DEFAULT_REGISTER_PASSWORD` | 不变（常量名） |
| 269, 270 | `password="1234@1234"` / `valid_password("1234@1234")` | `admin123` |
| 276, 277 | 后续优化中的 `1234@1234` 引用 | `admin123` |

## 执行方式

编辑 `plans/skip_reg_code_plan.md`，用 `admin123` 替换所有 `1234@1234` 出现处。
